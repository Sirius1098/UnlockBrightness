# JVM 参数：Android Studio 会使用其自带的 JDK（17 及以上）运行 Gradle。
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true

# 本项目不使用 AndroidX，也未声明任何 AndroidX 依赖。
android.nonTransitiveRClass=true
